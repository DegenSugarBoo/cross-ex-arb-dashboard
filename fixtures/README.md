# Fixtures

Representative REST/WebSocket payloads used by parser and exchange-adapter tests.

When an upstream payload contract changes, add the smallest representative fixture and a focused parser assertion alongside it. Fixtures contain public sample data only.
