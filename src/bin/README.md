# Utility binaries

- `perf_profile` — parser and engine throughput profile.
- `collector_codec_bench` — synthetic codec comparison.
- `ws_exchange_ingest_bench` — transport-plus-parser ingest benchmark.
- `ws_transport_bench` — `fastwebsockets` versus optional `tungstenite` comparison.
- `hyperliquid_probe` — short connectivity and frame sanity check.
