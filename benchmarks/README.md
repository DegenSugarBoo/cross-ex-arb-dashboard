# Benchmarks

This directory contains saved JSON snapshots for the engine, parser, collector-codec, and WebSocket-ingest checks.

Run the associated binaries or helper scripts from the repository root. Throughput is hardware- and toolchain-dependent; the snapshots are regression references, not universal performance claims.
