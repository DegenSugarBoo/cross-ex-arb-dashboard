# Assets

Screenshots used by the root README:

- `main_screen.png` — live scanner table and exchange status cards.
- `asset_30s_history.png` — selected-route 30-second history view.

These are presentation assets only; runtime data is collected from the configured exchange feeds.
